// SliceWindowTests.cpp — scaffold placeholder (T1). Real slice-window math
// tests (gent::resolveCueEndEdit / applyCueEdit / effectiveCueEnd /
// resolveEndDragTarget) are filled in by T2 — see TEST_TARGET_TASK.md.
#include "doctest.h"

TEST_CASE("scaffold: slice window suite compiles") { CHECK(true); }
