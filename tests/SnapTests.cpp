// SnapTests.cpp — scaffold placeholder (T1). Real snap-capture math tests
// (gent::nearestTransientIn / applySnapThreshold / selectSnapCursor) are
// filled in by T3 — see TEST_TARGET_TASK.md.
#include "doctest.h"

TEST_CASE("scaffold: snap suite compiles") { CHECK(true); }
