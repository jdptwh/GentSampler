// StemMaskTests.cpp — scaffold placeholder (T1). Real stem-mask semantics
// tests (gent::stemMaskWithBit / singleStemIndex / stemGainFor) are filled
// in by T5 — see TEST_TARGET_TASK.md.
#include "doctest.h"

TEST_CASE("scaffold: stem mask suite compiles") { CHECK(true); }
