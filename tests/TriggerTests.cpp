// TriggerTests.cpp — scaffold placeholder (T1). Real trigger/playback state
// transition tests (gent::latchPressTurnsOff / voiceGateFlag / chokeSilences /
// releaseApplies) are filled in by T4 — see TEST_TARGET_TASK.md.
#include "doctest.h"

TEST_CASE("scaffold: trigger suite compiles") { CHECK(true); }
